("describe-this-dir" 
"
This is the mount point of a query returning some 
text from sexp-structured file.
"
)
("what-is-used"
("SBCL" "Steel Bank Common Lisp compiler")
("FUSE" "File system in USEr space")
("CL-FUSE" "Common Lisp bindings for FUSE")
("CL-FUSE.meta-fs" 
"
Next layer of abstraction. Allows one to specify layout of file system 
via the macrosystem.
")
("CL-FUSE.query-fs" "
Even higher-level interface. You write queries to be loaded 
by special handlers.
")
)
